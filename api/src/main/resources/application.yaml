ktor:
  application:
    modules:
      - com.backlogbattlers.api.ApplicationKt.module
  deployment:
    # Hosts (Render, Azure, etc.) inject PORT; fall back to 8080 for local runs.
    port: "$PORT:8080"
