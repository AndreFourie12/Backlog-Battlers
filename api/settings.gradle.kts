// Standalone Gradle build, deliberately NOT included in the Android build at the repo root,
// so the AGP and Kotlin/JVM plugins never conflict and each has its own CI job.
rootProject.name = "backlog-battlers-api"
