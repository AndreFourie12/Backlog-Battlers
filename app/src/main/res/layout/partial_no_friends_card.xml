<?xml version="1.0" encoding="utf-8"?>
<!-- empty state shown until the user has friends to compete with -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_no_friends"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card"
    android:orientation="vertical"
    android:padding="@dimen/page_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="@dimen/empty_state_tile"
            android:layout_height="@dimen/empty_state_tile"
            android:background="@drawable/bg_tile_surface"
            android:importantForAccessibility="no"
            android:padding="12dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_users"
            app:tint="@color/accent"
            tools:ignore="ContentDescription" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_empty_title"
                style="@style/Text.CardTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:includeFontPadding="false"
                android:text="@string/no_friends_yet" />

            <TextView
                android:id="@+id/tv_empty_body"
                style="@style/Text.Caption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:includeFontPadding="false"
                android:text="@string/no_friends_body" />
        </LinearLayout>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/layout_empty_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_share_invite"
            style="@style/Widget.App.AccentButton"
            android:layout_width="0dp"
            android:layout_height="@dimen/cta_height"
            android:layout_weight="1"
            android:layout_marginEnd="6dp"
            android:text="@string/share_invite_link"
            android:visibility="gone"
            app:icon="@drawable/ic_arrow_right" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_find_by_username"
            style="@style/Widget.App.AccentButton"
            android:layout_width="0dp"
            android:layout_height="@dimen/cta_height"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:text="@string/find_by_username"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
